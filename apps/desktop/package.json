{
    "name": "@project-chroma/desktop",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist-electron/main.cjs",
    "scripts": {
        "build": "tsdown",
        "rebuild": "electron-rebuild -f -w better-sqlite3",
        "start": "electron .",
        "build": "tsdown",
        "rebuild": "bun run rebuild:electron && bun run rebuild:sharp",
        "rebuild:electron": "electron-rebuild -f -w better-sqlite3",
        "rebuild:sharp": "cd ./node_modules/sharp && bun run build",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@project-chroma/contracts": "workspace:*",
        "@project-chroma/utils": "workspace:*",
        "better-sqlite3": "^12.5.0",
        "electron": "^39.2.7",
        "piscina": "^5.2.0",
        "sharp": "^0.35.3",
        "uuid": "^13.0.0"
    },
    "devDependencies": {
        "@electron/rebuild": "^4.0.1",
        "@types/better-sqlite3": "^7.6.13",
        "@types/node": "catalog:",
        "tsdown": "^0.22.0"
    },
    "productName": "Project Chroma"
}
